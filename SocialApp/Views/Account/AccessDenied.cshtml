@{
    ViewData["Title"] = "Access denied";
}

<div class="auth-wrap">
    <div class="auth-card">
        <div class="auth-head">
            <h1>Access denied</h1>
            <p>You don't have permission to view this page.</p>
        </div>
        <a asp-controller="Home" asp-action="Index" class="btn btn-primary btn-block">Back to home</a>
    </div>
</div>
